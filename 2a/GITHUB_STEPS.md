# GitHub Workflow Notes

Prepared for: `Atharv Avhad`

This file belongs to `Assignment 2a` and covers only the GitHub/version-control part.

Use these commands after creating a GitHub repository from your own account:

```bash
git init
git config user.name "Atharv Avhad"
git config user.email "your-email@example.com"
git add .
git commit -m "Add Assignment 2a GitHub setup files"
git branch -M main
git remote add origin https://github.com/your-username/assignment-2a-github.git
git push -u origin main
```

Assignment split used in this repository:

- `2a` = GitHub
- `2b` = Docker
- `2c` = Angular
