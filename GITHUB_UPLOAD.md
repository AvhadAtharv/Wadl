# GitHub Upload

The local repository is already prepared with:

- branch: `wad-lab-projects`
- remote: `origin -> https://github.com/AvhadAtharv/FoodWebsite.git`
- latest commit: `Add web development lab assignments and project documentation`

## Push command

Run this from the project root:

```bash
git push -u origin wad-lab-projects
```

If Git asks for authentication, use your GitHub username and a Personal Access Token.
